# Ragie MCP Gateway Environment Variables
# Copy this file to .env and fill in your actual values

# WorkOS Configuration
# Get these from your WorkOS dashboard
WORKOS_API_KEY=your_workos_api_key_here
WORKOS_CLIENT_ID=your_workos_client_id_here
WORKOS_REDIRECT_URI=http://localhost:3000/auth/callback
WORKOS_DOMAIN=your_domain.com
# To generate use openssl: openssl rand -hex 32
WORKOS_COOKIE_PASSWORD=

# Target MCP Server Configuration
# URL of the MCP server this gateway will proxy to
TARGET_MCP_SERVER_URL=http://localhost:8080

# Server Configuration
# Environment (development, production, etc.)
NODE_ENV=development

# Optional: Server port (defaults are localhost:3000)
# PORT=3000
